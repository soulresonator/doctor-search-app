[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
pythonpath = ["src/backend"]
env = [
    "SECRET_KEY=test-secret-key-not-for-production",
    "DB_NAME=doctorapp_test",
    "DB_USER=nhat",
    "DB_PASSWORD=",
    "DB_HOST=localhost",
    "DB_PORT=5432",
]
