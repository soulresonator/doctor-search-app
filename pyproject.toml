[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
pythonpath = ["src/backend"]
env = [
    "D:SECRET_KEY=test-secret-key-not-for-production",
    "D:DB_NAME=doctorapp_test",
    "D:DB_USER=nhat",
    "D:DB_PASSWORD=",
    "D:DB_HOST=localhost",
    "D:DB_PORT=5432",
]
